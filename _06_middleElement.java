package linked_list;

public class _06_middleElement {
    public static void main(String[] args) {
        /*
         Node slow = head;
         Node fast = head;
         while(  fast!=null && fast.next!= null &&){
            slow = slow.next;
            fast = fast.next.next;
         }
         return slow
         */
    }
}
