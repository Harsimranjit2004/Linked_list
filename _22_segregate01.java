package linked_list;

public class _22_segregate01 {
    /*
     public static Node segregate(Node head){
        if(head == null || head.next == null) reutnr head;
        Node zero = new Node(-1)
        Node pz = zero;
        Node one = new Node(-1)
        Node po = one;

        Node curr =head ;
        while(curr!=null){
            if(curr.val == 0){
                pz.next = curr;
                pz = pz.next;
            }else{
                po.next = curr;
                po = po.next;
            }
            curr = curr.next;

        }
        pz.next = one.next;
        p0.next =null;
        reutrn zero.next;
     }
     */
}
