package linked_list;

public class _09_cycleListII {
    /*
     while(fast!=null){
        slow = slow.next;
        fast = fast.next.next;
        if(fast == slow) break;
     }
     Node tmep = head;
     while(temp!=slow){
        temp  = temp.next;
        slow  = slow.next;
     }
     return slow
     
     */
}
