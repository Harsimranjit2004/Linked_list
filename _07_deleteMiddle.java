package linked_list;

public class _07_deleteMiddle {
    public static void main(String[] args) {
        /*
         Node slow = head;
        Node fast = head;
        if(head.next == null){// only one element
            return null;
        }
        while(fast.next.next!=null && fast.next.next.next!=null){
            slow = slow.next;
            fast = fast.next.next;
        }
        slow.next = slow.next.next;
         */
    }
}
