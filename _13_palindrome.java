package linked_list;

public class _13_palindrome {
    /*
     public static Node reverse(Node head){
        Node prev = null;
        Node agla = null;
        Node current = head;
        while(current!=null){
            agla = cuurent.next;
            current.next = prev;
            prev = current;
            current = agla
        }
     }
     public static Node isPalindrom(Node head){
        Node slow = head;
        Node fast = head;
        while(fast.next!= null && fast.next.next!=null ){
            slow = slow.next;
            fast = fast.next.next;
        }
        Node temp = reverse(slow.next);
        slow.next = temp;
        node p1 = head;
        Node p2 = slow.next;
        while(p2!=null){
            if(p1.val!=p2.val){
                return;
            }
            p1 = p1.next;
            p2  = p2.next;
            
        }
        return ture;
     }
     */
    
}
