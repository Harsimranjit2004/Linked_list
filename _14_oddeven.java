package linked_list;

public class _14_oddeven {
    /*
     Node odd = new Node(0);
     Node even = new Node(0);
     tempo = odd; 
     tempe = even;
     temp = head;

     while(temp!=null){
        temp.next = temp;
        temp = temp.next;
        tempo  = tempo.next;
        if(temp==null) break // because maybe some time temp become null at lasst 
        tempe.next = temp;
        tmep= tmep.next;
        tmepe  = tempe.next;

     }
     odd = odd.next;
     even = even.next;
     tempo.next = even;
     */
}
