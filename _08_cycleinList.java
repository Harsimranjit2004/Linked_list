package linked_list;

public class _08_cycleinList {
    public static void main(String[] args) {
        /*
         Node slow = head;
         Node fast = head;
         if(head==null) return false
         if(head.next == null){
            return false;
         }
         while(fast!=null){
            if(slow == null) return false;

            slow = slow.next;
            if(fast.next == null) return fasle
            fast = fast.next.next;
            if(fast==slow) return true;
         }
         return false
         */
    }
}
